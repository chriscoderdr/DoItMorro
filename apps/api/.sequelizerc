const path = require("path");

module.exports = {
    config: path.resolve("dist", "database", "config", "sequelize-config-cli.js"), // Path to the config file
    "models-path": path.resolve("dist", "database", "models"), // Path to the models folder
    "seeders-path": path.resolve("dist", "database", "seeders"), // Path to the seeders folder
    "migrations-path": path.resolve("dist", "database", "migrations"), // Path to the migrations folder
};
